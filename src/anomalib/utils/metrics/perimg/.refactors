Refactors to make at the final PR of the feature branch.

1. summary of my changes to the CHANGELOG
2. review codacy suggestions
3. add functional interface to `__init__.py`
4. `_validate*` at the begining of modules
5. move `502_perimg_metrics.ipynb` to the folder `502_perimg_metrics`
6. rename PImO to PIMO (upper `M` instead of lower `m`)? 
7. fix notebook kernel
8. clean tmp sections in notebooks (at the end)

From https://github.com/openvinotoolkit/anomalib/pull/1247

1. rename `perimg` to `per_img`
  1.1 in functions
  1.2 the module
2. rename`common.py` to `utils.py`
3. add license header to files
4. remove `.ignore`
5. rename `atleast` to `at_least` (?)
